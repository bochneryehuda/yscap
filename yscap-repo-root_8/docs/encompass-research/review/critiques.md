# Critic 1 (completeness)

Verification complete. Findings below, based on full reads of all six drafts, title/scope review of all 31 findings files, targeted reads of F4/F5/E2/C4/D2, and repo spot-checks (advancementBlockers at `src/routes/staff.js:3895`, internal-status at `:4298`, `sync_queue` CHECK at `db/schema.sql:306-307`, `db/005:74-84` — all cited references check out).

1. **BLOCKER — ENCOMPASS-IDEAS-AND-ROADMAP §5 vs ENCOMPASS-INTEGRATION-RESEARCH §4.2, ENCOMPASS-READONLY-GUARDRAILS §6.2, ENCOMPASS-API-ATLAS §7.4.** The docs contradict each other on the single most gate-critical mechanic: three docs mandate a **live on-demand re-fetch at CTC decision time** ("hard-fails if it can't"), while IDEAS/F4 mandates **no network I/O in the request path by construction** (evaluator "holds no HTTP client"; live refresh explicitly rejected for Phase 1 and parked as a Phase-2 relaxation). The underlying findings (F5 FM-16 vs F4 §1) never converged and the drafts published both designs. Fix: pick one posture (or declare the CTC gate the single sanctioned live-read exception to the no-I/O rule) and align all four docs explicitly, citing the decision.

2. **MAJOR — ENCOMPASS-INTEGRATION-RESEARCH §5.3/§5.4 + API-ATLAS §6.5 vs IDEAS §3/§9 Q3 + DATA-MAPPING §8 Q17.** Phase-1 transport contradiction: the master's sync engine includes webhooks as one of five Phase-1 triggers with subscription setup as a sanctioned config write, while IDEAS states "Phase 1 is **poll-only**" and treats webhook subscriptions as an unresolved owner decision (recommending polling-only). Fix: one recommendation with the owner decision point clearly marked; if webhooks are Phase-1, IDEAS' foundations table must say so.

3. **MAJOR — ENCOMPASS-DATA-MAPPING §2 (rank 2) and §4 (step 1) vs GUARDRAILS §5.1 Tier 3 and INDUSTRY-LANDSCAPE §5.4.** The matching/enrichment design leans on `ssn_hash` as the strong borrower key ("strong SSN match attaches"), but Guardrails recommends launching with Tier 3 (SSN/DOB pull) **disabled** and Industry says "Pull no full SSN or DOB from Encompass in Phase 1." If Tier 3 launches disabled, the enrichment stream's primary attach key does not exist — DATA-MAPPING never acknowledges this dependency or states the degraded fallback match strategy. Fix: DATA-MAPPING §2/§4 must state the Tier-3-disabled default and rank the non-SSN fallback ladder as primary for launch.

4. **MAJOR — schema divergence across four docs.** Master §5.4 (`encompass_loan_snapshot`, `encompass_loan_link` with 7 states incl. `conflict`/`data_only`), GUARDRAILS §5.2 (`encompass_loan_index`, `encompass_loan_state`, `encompass_snapshots`, `encompass_pull_log`, `encompass_gate_log`), DATA-MAPPING §2.1 (`encompass_loan_index` with 5 different states incl. `review_pending`), IDEAS §3 (`encompass_loans`/`encompass_conditions`/`encompass_milestones` + `borrower_encompass_links`) each propose different tables and state models for the same objects; binding location also conflicts (link-table row vs `applications.encompass_loan_guid` column), and Master Stage 2's "no application/borrower columns touched" is incompatible with setting `applications.encompass_loan_guid` at match time. Fix: declare one canonical schema (suggest in DATA-MAPPING), make the other docs reference it, and reconcile the Stage-2 wording.

5. **MAJOR — ENCOMPASS-INTEGRATION-RESEARCH §8 vs IDEAS §7.** Two different roadmaps: Master stages 0–5 vs IDEAS Phases 0–3 with different content — borrower milestone timeline is IDEAS Phase 2 but Master Stage 5 "Later"; IDEAS Phase 3 includes the **eFolder document push (L6), a write**, which appears nowhere in the Master roadmap and sits in tension with the master's "never the reverse" framing (Guardrails' unfreeze ceremony reconciles it, but the master never acknowledges L6). Fix: one roadmap of record in the master; IDEAS' phase plan should map onto it, and the master should explicitly acknowledge (or explicitly reject) the Phase-3 write candidates.

6. **MAJOR — DATA-MAPPING §5.4 vs IDEAS §5 (and E2 rule B1).** Door-count disagreement on the CTC gate: DATA-MAPPING says the gate "must cover all **three** doors" (status PATCH, internal-status, ClickUp-inbound), IDEAS/F4 covers **two** doors, and E2's B1 explicitly says a ClickUp-inbound CTC status is "flagged, not blocked." Fix: a single decision (recommend block at all three, per DATA-MAPPING) recorded once and mirrored in the other docs.

7. **MAJOR — ENCOMPASS-INTEGRATION-RESEARCH §6 vs GUARDRAILS §0/§8 Q1.** Factual contradiction about the day-one blocker: the master asserts "**A credential was shared in a chat message during this project**" as fact, while Guardrails says whether any Encompass credential transited an insecure channel is "**unconfirmed** — Open Question 1." Both converge on rotate-anyway, but the docs of record disagree about what happened. Fix: state the same verified fact in both (and if the master's claim is true, Guardrails' open question is already answered).

8. **MAJOR — ENCOMPASS-INTEGRATION-RESEARCH §10 ("Open questions — consolidated").** The claimed consolidation is incomplete: it omits IDEAS §9's owner decisions (auto-verify funded-loan track records, borrower-facing gating, outage/fail-closed tolerance duration, tolerance culture), INDUSTRY §8's counsel items (licensed-state list, 5,000-consumer Safeguards threshold, signed-agreement data-replication language), and Guardrails Q11 (Qualified Individual / retention sign-off). The owner has no single deduplicated tracker; each doc numbers its own overlapping list. Fix: make Master §10 a true superset with stable IDs, and have the sibling docs cite those IDs.

9. **MAJOR — ENCOMPASS-INTEGRATION-RESEARCH §1/§4 vs IDEAS §5 and DATA-MAPPING §5.3.** Expectation gap on a headline mission rule: the master promises unqualified "a portal condition cannot be cleared unless Encompass agrees," but the actual design gates **only explicitly mapped, opt-in conditions** ("unmapped portal conditions are simply not gated"; `encompass_gate` NULL = today's behavior). Fix: the master's exec summary should say "a *mapped* portal condition…" and state the expected initial mapping coverage (the ≥90% coverage gate in IDEAS §7 helps).

10. **MINOR — GUARDRAILS §2 (also Master §5.2, Atlas §11, Industry §7).** Layer-count arithmetic: Guardrails' header says "five independent layers" over a table numbered 0–5 (six rows); Master says five (0–4 plus an unnumbered DB constraint); Atlas summarizes "two layers," Industry "three layers." Fix: settle on one count and naming in Guardrails (the doc "every future session must read") and label the others as condensed views.

11. **MINOR — Master §5.3 vs Atlas §5.** Estimated daily call volume differs (~850–1,750/day vs ~500–1,500/day) with no explanation of the delta. Fix: one estimate with its assumptions, referenced from both.

12. **MINOR — INDUSTRY-LANDSCAPE §4.1 lesson 1.** Presents the 30-min/24-h token regime as settled fact and says the token manager "refreshes ~5 min early," contradicting Atlas §2.2 (two conflicting documented regimes; no refresh token exists — "refresh" = re-authenticate). Fix: align with the design-for-the-stricter framing and re-auth semantics.

13. **MINOR — IDEAS §0.** Understates the incident record ("13 guard/incident documents and one data-corruption incident") vs the 16-incidents/9-days history in Master §2 and Guardrails §3 — weakening the safety rationale in the doc the owner reads for the fun part. Fix: use the same 32-minutes/16-incidents framing.

14. **MINOR — INDUSTRY §6.1 vs Master §6 step 2 / Atlas §2.4.** Industry describes API service accounts as "Encompass users with an 'API User' flag" while the provisioning instructions elsewhere say explicitly **do NOT check the API User box** (it breaks the lender password grant). An admin reading only Industry would do the opposite of the instruction. Fix: add a cross-reference/warning in Industry §6.1.

15. **MINOR — Atlas §6 / Master §5.3.** ICE's **auto-deletion of failing webhook subscriptions** (D2 finding; the integration "silently goes deaf") appears only in Industry §4.1 lesson 6 — it is missing from the Atlas webhook section and the master's sync design, where it is the strongest justification for the subscription-drift read check. Fix: add one sentence to Atlas §6.3.

16. **MINOR — completeness gap (DATA-MAPPING §1, Master roadmap).** No transition/decommission plan for the superseded surfaces: the hand-typed `applications.encompass_status` ClickUp mirror column and the five manual "check in Encompass" checklist tasks (`db/005:74-84`) that the integration auto-verifies. "The real integration supersedes it" is stated, but no roadmap stage retires or re-labels them. Fix: add a decommission step (likely Stage 3/4) to the master roadmap.

Clean categories (one line each): read-only scope and minute-one write freeze are thoroughly and consistently policy-stated; ClickUp/SharePoint guard modeling is deeply mined (A1–A6) and reused; manual review checkpoints are designed as a system; the 800-request GET/write/delete classification (C7/Atlas) fully satisfies "understand every call"; webhooks are mastered (payloads, HMAC, event history, EFC, reconciliation); industry research and idea catalogs are comprehensive; plain-language/user-friendliness sections are present in all six docs; a day-one owner action list exists (Master §6 + Stage 0), subject only to issue 8's consolidation gap; no research angle in the findings directory is absent — all 31 findings are reflected in the drafts.

# Critic 2 (accuracy)

Verification complete. I spot-checked 40+ claims across the six drafts against the repo, the Postman index, the classification JSON (`c7-classified.json`), and the raw uploaded collection. Findings:

**Issues (ordered by severity):**

1. **MAJOR — ENCOMPASS-INTEGRATION-RESEARCH.md §6 vs ENCOMPASS-READONLY-GUARDRAILS.md §0/§8 — contradictory claim about a leaked credential.** The master doc asserts as fact: "A credential was shared in a chat message during this project" and makes rotation "step 1 of everything below." The guardrails doc says the opposite: "Whether an Encompass credential has in fact transited an insecure channel is unconfirmed — it is Open Question 1." No findings file (A4, A5, C1, F3, F5) supports the affirmative claim — they all cite only the 2026-07-12 SharePoint incident (`docs/SHAREPOINT-INTEGRATION-RESEARCH.md:28-42`) and the standing CLAUDE.md rule. This looks like conflation with the SharePoint precedent, i.e., a fabricated incident in the executive document. Fix: either cite the actual chat event with date/source, or soften the master doc to the guardrails wording ("unconfirmed; treat as burned and rotate anyway").

2. **MAJOR — CTC-gate freshness design contradiction across three drafts.** Master §4.2 and Guardrails §6.2 require "a live on-demand re-fetch at decision time" for CTC (Atlas §7.4: "a live (not pipeline) read, and a freshness TTL (≤5 min)"), while IDEAS-AND-ROADMAP §5 specifies the opposite: "No network I/O in the request path — by construction. The evaluator module holds no HTTP client" (live refresh explicitly parked as a Phase-2 question, Q14). This is the single most gate-critical behavior and an implementer will find two mutually exclusive specs. Fix: pick one design (or explicitly mark the F4-vs-E2/F5 divergence as an open decision in all three docs).

3. **MAJOR — storage schema and crosswalk state model diverge across the doc set.** Master §5.4: `encompass_loan_snapshot` + `encompass_loan_link` with states `auto_matched/manual_confirmed/unmatched/conflict/ambiguous/data_only/ignored`; Guardrails §5.2: `encompass_loan_index`/`encompass_loan_state`/`encompass_snapshots`/`encompass_pull_log`/`encompass_gate_log` (plus §2.5's `encompass_request_log`); DATA-MAPPING §2.1: `encompass_loan_index` with states `unmatched/matched/ambiguous/review_pending/ignored`; IDEAS §3/§5: `encompass_loans`/`encompass_conditions`/`encompass_milestones` + `encompass_gate_decisions`. The guardrails gate precondition ("match status ∈ {auto_matched, manual_confirmed}") references states that don't exist in DATA-MAPPING's model. Fix: one canonical table/state appendix, referenced by the other five docs.

4. **MINOR — ENCOMPASS-DATA-MAPPING.md §3.1 — wrong schema line citations.** `loan_amount` cited at `db/schema.sql:177` (line 177 is `purchase_price`; `loan_amount` is line 187) and `ltv` at `:181` (line 181 is `rehab_type`; `ltv` is line 188). The `ys_loan_number:164` cite is correct. Fix the two line numbers. (Same class, trivial: crypto.js NYDFS comment is at :164 not :167; `redactPII` at redact.js:9 not :7; §1's "124 idempotent migrations" — actual count is 126 SQL files in `/home/user/yscap/yscap-repo-root_8/db/`.)

5. **MINOR — ENCOMPASS-IDEAS-AND-ROADMAP.md §3 (Local mirror row) — stale borrower.js line citations.** "S2-04/S2-06 leak surfaces (`src/routes/borrower.js:272-315`, `:1134-1141`)" — in the current repo line 272 is the photo-ID upload and 1134 is `PATCH /llcs/:id`. The actual send-everything surfaces are `GET /applications/:id` (`SELECT a.*`, now ~line 411) and the track-record `SELECT t.*` (~line 1680). Numbers were copied from the older portal-audit doc. Fix: re-cite by route name, not line.

6. **MINOR — Master §7 and Guardrails §6 — risk-register count is wrong.** Both say "F5 catalogs **27** risks (FM-01…FM-44)"; the F5 findings file contains **29** unique FM rows (FM-01…FM-44 with gaps). Fix the number.

7. **MINOR — Master §5.3 vs Atlas §5 — conflicting call-volume estimates.** "~850–1,750 calls/day" (master) vs "~500–1,500 calls/day" (atlas). Both are estimates from different findings (F2 vs C3); harmonize or attribute the divergence.

8. **MINOR — Atlas §10.1 B6 vs Guardrails §5.1 Tier 4 — `conversationLogs` allowlisted but excluded by governance.** The runtime allowlist includes `GET .../conversationLogs` while data governance excludes "free-text conversation logs" (Tier 4 EXCLUDE, persona-denied). Fix: drop it from B6 or annotate why an excluded data class keeps a callable endpoint.

9. **MINOR — IDEAS §0 — incident-count inconsistency.** "the two-way ClickUp sync left 13 guard/incident documents and **one data-corruption incident** behind" contradicts the 16-incident history every other doc cites (verified: `docs/AUDIT-2026-07-15-SYNC-ANNUAL.md` §3, "16 incidents, forensically reconstructed"). Fix the sentence.

10. **MINOR — IDEAS §3/§9 Q3 vs Master §5.3 — Phase-1 webhook posture differs.** IDEAS says "Phase 1 is **poll-only**," webhooks an unresolved owner question; the master's Phase-1 sync engine includes webhook delivery as one of five triggers and sanctions subscription setup. Both agree polling is the source of truth, but build scope differs. Fix: state one Phase-1 posture.

11. **MINOR — Guardrails §2 — "five independent layers" table has six rows (0–5).** The master doc handles this as "five layers plus a DB constraint"; the guardrails table folds the DB constraint in as row 5 under a "five layers" heading. Align the numbering.

12. **MINOR — Atlas §4.3/§7.2 small precision slips.** (a) "PRECON.* (19 fields)" — the raw collection sample contains 18 unique `PRECON.*` IDs (UWC 20 ✓, LOCKRATE 17 ✓); (b) `ENHANCEDCOND.X1` appears **nowhere in the collection** (0 hits in the raw JSON) — it is docs-sourced (C5's reference link), so the table heading "verified in collection samples and public docs" should mark it docs-only; (c) §7.2 abbreviates the export path as `POST /efolder/v1/exportJobsCreator` while the real path (correct in §10.1 G7) is `/efolder/v1/loans/{loanId}/exportJobsCreator` — that endpoint's collection variant also carries a `skipPersonaChecks` query param the allowlist's query validation should explicitly pin/deny (not mentioned anywhere).

**Categories verified clean:**
- **Postman classification arithmetic: clean.** 800 requests; method counts (POST 200 / GET 329 / PUT 27 / PATCH 214 / DELETE 30), class counts (READ 329, READ_VIA_POST 49, WRITE_LOAN 318, WRITE_CONFIG 92, AMBIGUOUS 8, AUTH 4), the per-folder table in Atlas §9.3, the "53 of 200 POSTs are read/auth" and "~147 writes" figures, and the identities of all 8 AMBIGUOUS requests all match `c7-classified.json` exactly.
- **Allowlist internal consistency: clean** (except #8 above). No WRITE_LOAN or WRITE_CONFIG endpoint is on the runtime allowlist; `fieldReader`, `loanPipeline`, `auditTrail`, `attachmentDownloadUrl`, and `exportJobsCreator` are all correctly READ_VIA_POST; the two `action=Add` GETs, the "Get Authcode for Loan Recipients" GET, `fieldWriter`, `loanBatch/updateRequests`, and `resourceLocks` all exist as described and are correctly denied; webhook subscription CRUD is properly segregated to the admin-credential §10.2 table.
- **Repo claims: overwhelmingly accurate.** All checked file paths, migration numbers (005/022/023/027/037/044/047/048/056/069/081/083/107/108/110/112/114/123/124/125), guard behaviors (`guardNoTaskDeletion`, `guardTaskUpdatePayload`, `signOffGate` at staff.js:2400, `advancementBlockers` at :3895, the internal-status door at :4298 genuinely bypassing blockers, sync-review endpoints :5929-6167), the 11-value status lifecycle, the `db/056` relabels, and the ClickUp incident numbers (32 min, 16 incidents, 491 commits, 13 deploys, ~45 guards, 10 DOBs, year-0026, the 8-field wipe, "The ONLY protection is no deletion") all verify against `/home/user/yscap/yscap-repo-root_8`.
- **Credential-looking strings: none found** in any draft; the uploaded Postman environment (`/root/.claude/uploads/.../517c3b02-Encompass_Developer_Connect_postman_environment.json`) is placeholder-only, as the Atlas claims.
- **Write-freeze violations in recommendations: none.** The only proposed writes (webhook subscription config; IDEAS L6/L7 document push) are explicitly out-of-band/admin-credential or Phase-3 behind owner sign-off ceremony, consistent with the stated freeze exception process.
