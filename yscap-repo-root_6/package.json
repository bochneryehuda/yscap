{
  "name": "ys-capital-portal",
  "version": "0.2.0",
  "private": true,
  "description": "YS Capital borrower portal backend — Postgres + auth + notifications, serving the existing static site. ClickUp/Encompass sync layered on later.",
  "main": "src/server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "RUN_SYNC=1 node src/server.js",
    "migrate": "node db/migrate.js",
    "sync": "node -e \"require('./src/sync/queue').start()\""
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
